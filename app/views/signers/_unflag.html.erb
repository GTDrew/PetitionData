<p>Flagged For: <%= signer.flag_purpose %></p>
  <%= form_for signer do |f| %>
  <div class='form-group text-center'>
    <%= f.submit 'Unflag Signer', class: 'btn btn-danger' %>
  </div>
  <% end.gsub('"', '\'') %>
